# 📱 Как создать APK для Android

## ✅ Что уже подготовлено:

1. **Capacitor инициализирован** ✓
2. **Android проект создан** ✓
3. **Фронтенд собран** ✓

Папка: `/android` готова для сборки

## 🔧 Два способа создать APK:

### Способ 1: На локальной машине (РЕКОМЕНДУЕТСЯ)

1. **Установи на компе:**
   - Java Development Kit (JDK 17+)
   - Android Studio
   - Android SDK

2. **Клонируй проект:**
   ```bash
   git clone https://github.com/Daothecreator/Zero-Azimuth.git
   cd Zero-Azimuth
   ```

3. **Установи зависимости:**
   ```bash
   npm install
   npm run build
   ```

4. **Открой проект в Android Studio:**
   - File → Open → выбери папку `android/`

5. **Собери APK:**
   - Build → Build Bundle(s) / APK(s) → Build APK(s)
   - Или через терминал:
   ```bash
   cd android
   ./gradlew assembleDebug    # Debug версия
   ./gradlew assembleRelease  # Release версия
   ```

### Способ 2: Через EAS Build (Expo)

```bash
npm install -g eas-cli
eas login
eas build --platform android
```

## 📁 Где будет APK?

После сборки APK будет здесь:
```
android/app/build/outputs/apk/debug/app-debug.apk
```

## 📤 Отправить на телефон:

1. **Через USB:**
   - Подключи телефон USB кабелем
   - `adb install app-debug.apk`

2. **Через Telegram/Email:**
   - Отправь файл `app-debug.apk`
   - Открой на телефоне и установи

3. **Через QR код:**
   - Залей APK на сервер
   - Сгенерируй QR код на URL
   - Отсканируй на телефоне

## ⚠️ Важно:

- **Debug APK**: можно ставить только в режиме разработки
- **Release APK**: готово к публикации в Play Store
- Для Release нужен подписанный ключ (keystore)

## 🎯 Статус проекта:

- ✅ Capacitor готов
- ✅ Android проект создан
- ✅ Код оптимизирован
- 🟡 Требуется Java 17+ для финальной сборки

